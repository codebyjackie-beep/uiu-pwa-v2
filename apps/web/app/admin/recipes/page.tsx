import { LiveRecipesAdmin } from "./LiveRecipesAdmin";

export const metadata = {
  title: "Live Recipes Admin — UIU",
};

export default function LiveRecipesAdminPage() {
  return <LiveRecipesAdmin />;
}
